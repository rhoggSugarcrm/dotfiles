# for colors
export CLICOLOR=1
export LSCOLORS=ExFxCxDxBxegedabagacad

# grep colors
export GREP_OPTIONS='--color=auto'

# alias for listing
alias ll='ls -lah'
alias la='ls -a'
alias l='ls -lh'

# alias for disk tools
alias du='du -h'
