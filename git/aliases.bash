alias "ga"="git add";
alias "gb"="git branch";
alias "gc"="git commit";
alias "gd"="git diff";
alias "gp"="git push";
alias "gs"="git status -sb";
alias "GS"="gs"; # because by default this will launch GhostScript on macOS
